---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: vdbench
  name: vdbench-pvc
  namespace: benchmark
spec:
  storageClassName: cvs-standard
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Ti